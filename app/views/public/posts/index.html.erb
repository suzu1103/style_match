<h1>タイムライン</h1>
<div class ="row">
  <% @posts.each do |post_image| %>
    <div class="card-deck">
      <div class="card" style = "width:250px">
    
          <!--%= link_to item_path(item.id) do %>-->
            <%= image_tag post_image.get_image, width: '50', height: 'auto', class:"card-img-top"; %>
          <!--% end %>-->
          <div class="card-body">
            <p class="card-text"><%= post_image.introduction %></p>
          </div>
      </div>
    </div>
  <% end %>
</div>